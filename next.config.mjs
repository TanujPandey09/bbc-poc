/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['ichef.bbci.co.uk','tpc.googlesyndication.com'], // If you want to keep using BBC images
      }
};

export default nextConfig;
